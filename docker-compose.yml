version: "3.8"

services:
  db:
    image: pgvector/pgvector:pg15
    environment:
      POSTGRES_USER: jobhunt
      POSTGRES_PASSWORD: jobhunt_dev
      POSTGRES_DB: jobhunt
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
